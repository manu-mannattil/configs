#!/bin/sh

case "$1" in
    -*) exec /usr/bin/google-chrome "$@" ;;
esac

exec firejail                        \
    --whitelist=~/.config/fontconfig \
    --whitelist=~/downloads          \
    /usr/bin/google-chrome           \
    --password-store=basic           \
    --incognito "$@"
