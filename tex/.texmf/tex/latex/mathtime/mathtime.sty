% mathtime.sty -- MathTime Pro 2 fonts with some customizations
%
% Requires: mtpro2, helvet
%

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{mathtime}[2019/01/01 v0.2]

% Times for Roman, CM Typewriter for fixed-width text, and
% Helvetica for sans serif.
\RequirePackage{ifxetex,ifluatex}
\ifnum 0\ifxetex 1\fi\ifluatex 1\fi=0 % if pdftex
  \renewcommand{\rmdefault}{ptm}
  \renewcommand{\ttdefault}{cmtt}
  \RequirePackage[scaled=0.92]{helvet}
\else % if luatex or xelatex
  \RequirePackage[no-math]{fontspec}
  \setmainfont{TeX Gyre Termes}
  \setsansfont[Scale=0.92]{TeX Gyre Heros}
\fi

% To avoid conflicts with amssymb and Springer's svjour.
\let\Bbbk\relax
\let\heavymath\relax
\let\vec\relax

\RequirePackage[
  morphedbraces,
  mtphrb,
  mtpcal,
  nofontinfo,
  subscriptcorrection,
  zswash
]{mtpro2}

\renewcommand\hbar\hslash % better reduced planck's constant

\endinput
